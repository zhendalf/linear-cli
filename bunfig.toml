[test]
timeout = 30000
# Run tests sequentially to avoid process.env/process.argv races in snapshot tests
maxConcurrency = 1
# Pin a deterministic, terminal-independent environment (no color, non-TTY,
# fixed width) so tests pass identically piped and in an interactive terminal.
preload = ["./test/setup.ts"]
